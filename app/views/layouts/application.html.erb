<!DOCTYPE html>
<html>
  <head>
    <title>ScheduleBook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <script src="https://kit.fontawesome.com/16220d5f5d.js" crossorigin="anonymous"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'home' %>
  </head>

  <body>
    <header>  
      <div class="header-menus">
        <div><%= link_to(image_tag("header-logo.png", class: "header-logo"), '/') %></div>
        <% if user_signed_in? %>
          <div id="menuToggle">
            <div><%= image_tag(current_user.profile_image.url, size: '30x30') %></div>
            <div id="menuIcon"><%= current_user.name %><i class="fa-solid fa-chevron-down"></i></div>
          </div>

          <div id="menu">
            <p><%= link_to("施設の新規登録", new_room_path) %></a>
            <p><%= link_to("アカウント設定", '/users/account' ) %></p>
            <p><%= link_to("予約一覧", reservations_path) %></p>
            <p><%= link_to("登録一覧", '/rooms/own') %></p>
            <p><%= link_to("ログアウト", destroy_user_session_path, method: :delete) %></p>
          </div>
        <% else %>
          <div>
            <%= link_to("ログイン", new_user_session_path) %>
            <%= link_to("新規登録", new_user_registration_path) %>
          </div>
        <% end %>
      </div>
    </header>
    <%# フラッシュメッセージ %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <footer>

    </footer>
  </body>
</html>
